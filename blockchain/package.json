{
  "name": "deedchain-contracts",
  "version": "1.0.0",
  "description": "DeedChain Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network lineaTestnet",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "verify": "hardhat verify --network lineaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1",
    "hardhat": "^2.17.0",
    "hardhat-gas-reporter": "^1.0.9"
  }
}